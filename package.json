{
  "name": "time-based-http-redirector",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy --minify",
    "cf-typegen": "wrangler types --env-interface CloudflareBindings",
    "migrate:local": "wrangler d1 migrations apply rust-reading-group-nfc-redirects-db --local",
    "migrate:prod": "wrangler d1 migrations apply rust-reading-group-nfc-redirects-db --remote"
  },
  "dependencies": {
    "@js-temporal/polyfill": "^0.5.1",
    "dotenv": "^17.2.3",
    "drizzle-orm": "^0.44.7",
    "hono": "^4.10.4",
    "zod": "^4.1.12"
  },
  "devDependencies": {
    "@biomejs/biome": "2.3.4",
    "@types/node": "^24.10.0",
    "drizzle-kit": "^0.18.1",
    "wrangler": "^4.4.0"
  }
}
